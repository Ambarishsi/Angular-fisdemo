import { HomeRendererDirective } from './home-renderer.directive';

describe('HomeRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new HomeRendererDirective();
    expect(directive).toBeTruthy();
  });
});
